#pragma once
ref class RetCoord
{
public:
	int left;
	int right;
	int top;
	int bottom;

	RetCoord(int new_left, int new_right, int new_top, int new_bottom) { left = new_left; right = new_right; top = new_top; bottom = new_bottom; }
};

